package com.gymnastic.ecommerceapp.ui.theme

import androidx.compose.ui.graphics.Color

// Esquema de colores moderno para e-commerce (azul/naranja)
// Colores para tema oscuro
val Blue80 = Color(0xFF90CAF9) // Azul claro para tema oscuro
val Orange80 = Color(0xFFFFB74D) // Naranja claro para tema oscuro
val Grey80 = Color(0xFFB0BEC5) // Gris claro para tema oscuro

// Colores para tema claro
val Blue40 = Color(0xFF1976D2) // Azul principal para tema claro
val Orange40 = Color(0xFFFF6F00) // Naranja principal para tema claro
val Grey40 = Color(0xFF424242) // Gris oscuro para tema claro

// Colores adicionales para la app
val LightBlue = Color(0xFFE3F2FD) // Azul muy claro para fondos
val LightOrange = Color(0xFFFFF3E0) // Naranja muy claro para fondos
val DarkBlue = Color(0xFF0D47A1) // Azul muy oscuro para acentos
val DarkOrange = Color(0xFFE65100) // Naranja oscuro para acentos